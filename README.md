# GPT-based-Small-Language-Model
A custom GPT-based Small Language Model (SLM) trained on Tinystories dataset utilizing PyTorch framework to construct the transformer architecture, optimize the cross-entropy loss function

Used Lightning Ai for GPU for training the model.
